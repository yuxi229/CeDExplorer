# Global variable definitions to avoid R CMD check notes
utils::globalVariables(
  c(
    "celiac_gwas_example", "example_expression", "example_expression_matrix",
    "example_metadata", "ppi_data", "condition", "expression", "p_value",
    "position", "frequency", "score", "degree", "type", "importance", 
    "weight", "label", "term_name", ".data"
  )
)
